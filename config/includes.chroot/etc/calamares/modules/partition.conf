---

# Match the Arch edition's explicit automated-install swap choices. More than
# one supported choice makes Calamares show the selector on the Erase disk
# workflow. Keep no swap as the safe default; "small" creates a bounded swap
# partition without promising hibernation support.
userSwapChoices:
    - none
    - small

initialSwapChoice: none
