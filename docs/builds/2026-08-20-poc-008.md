# POC Build 008

## Result

- Status: Successful build; runtime acceptance passed with one visual finding
- Build date: 2026-08-20 America/Chicago
- Configuration commit: `c199d6e`
- Base: Debian 13.6 (`trixie`)
- Architecture: `amd64`
- live-build version: `20250505+deb13u1`
- Build duration: approximately 11 minutes 50 seconds

Build 008 is limited to the three presentation findings retained after Build
007 acceptance. It remains a proof-of-concept image rather than a supported
release.

## Artifact

| Property | Value |
| --- | --- |
| Filename | `oblinux-debian-gnome-amd64.hybrid.iso` |
| Exact size | 2,135,902,208 bytes |
| Approximate size | 1.99 GiB / 2.14 GB |
| Volume ID | `OBLINUX_LIVE` |
| Package count | 1,766 |
| SHA-256 | `ca1534f99791585e97b8759a36d240c25207fb37816d58a5a604767ffb59dd78` |

The ISO, checksum, content and package manifests, file list, and build log are
preserved outside Git under the Build 008 artifact archive.

The image is 11,581,440 bytes, approximately 11.0 MiB, below GitHub Releases'
2 GiB per-file limit.

## Change from Build 007

- Added `quiet splash` to the normal live kernel command line so the embedded
  Plymouth theme can provide the graphical presentation.
- Replaced the package-owned `install-debian.png` Dash launcher asset with the
  accepted transparent Calamares symbol.
- Added a build-time byte comparison between the Calamares and Dash assets.
- Declared Debian's expected 128-by-128 intrinsic size on the active GNOME
  vendor emblem without changing the approved symbol geometry or colors.

## Static verification

The following checks passed against the generated build tree and ISO staging
area:

- `live-build` reported `Build completed successfully`.
- The generated normal live GRUB entry contains `boot=live components quiet
  splash` followed by all existing live-session parameters.
- No stale normal live entry lacking `quiet splash` was found.
- The Dash launcher continues to resolve `Icon=install-debian`.
- The resolved `install-debian.png` is a 256-by-256 RGBA PNG and is
  byte-identical to the accepted transparent Calamares symbol.
- The active GNOME vendor emblem declares a 128-by-128 intrinsic size.
- The active emblem matches the canonical OBLinux vendor asset, the diversion
  remains registered, and Debian's original emblem remains preserved.
- The OBLinux Plymouth configuration, descriptor, script, and corrected logo
  remain embedded in the generated initramfs.
- The package manifest remains at 1,766 packages.
- The archived ISO checksum matches the original build output.

## Runtime verification

Runtime acceptance passed for live GRUB and Plymouth, GNOME startup, the Dash
launcher icon, Calamares, GNOME About identity and sizing, installation,
cleanup, time synchronization, APT, installed GRUB and Plymouth configuration,
and general system function. GNOME About's Obsidian-colored letters have
insufficient contrast in dark appearance. See
[Installation Test 01](../tests/2026-08-20-poc-008-install-01.md).
