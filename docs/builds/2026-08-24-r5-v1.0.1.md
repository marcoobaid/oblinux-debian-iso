# Brand Master v1.0.1 development build

## Result

- Status: ISO build and static payload inspection successful
- Build date: 2026-08-24 America/Chicago
- Source basis: `8a6d275` plus the R5 v1.0.1 correction commit containing
  this record
- Base: Debian 13 (`trixie`)
- Architecture: `amd64`
- live-build version: `20250505+deb13u1`
- Runtime status: not tested by Codex; visual acceptance required

This is a development/staging artifact. It is not a stable release and is not
approved for promotion.

## Branding dependency

| Property | Value |
| --- | --- |
| Brand Master release | `v1.0.1` |
| Release commit | `4e0ddbc9b8a98b5717c199c33887cc458480afb2` |
| Commit archive SHA-256 | `1b5e620859250ac3585198ce3e6541c70d979bb29234005872c53cd08fa47116` |
| Debian package | `oblinux-branding` `1.0.1-1` |

The annotated release tag resolved to the expected commit. The immutable
commit archive matched the locked checksum, the package built successfully,
and Brand Master's internal asset validation passed.

## Artifact

| Property | Value |
| --- | --- |
| Filename | `oblinux-debian-gnome-amd64.hybrid.iso` |
| Builder path | `/home/marco/oblinux-debian-iso-dev/oblinux-debian-gnome-amd64.hybrid.iso` |
| Exact size | `2,183,051,264` bytes |
| Volume ID | `OBLINUX_LIVE` |
| SHA-256 | `daf68de5830a68f4f01de2b8212f7b338168be072ecd931870aad02ab3f127fb` |

The previous v1.0.0 development ISO was preserved separately before the clean
build. Binary artifacts and logs remain outside Git.

## Validation completed

- `scripts/validate-branding-integration` passed locally and on the builder.
- `lb config --validate` passed.
- The Brand Master package built from the verified v1.0.1 commit archive.
- Brand Master's validator passed 23 required assets and 39 SVGs.
- The Horizon derivative unit test and package build passed.
- Every ordered OBLinux chroot hook passed.
- `oblinux-branding` `1.0.1-1` and `oblinux-icon-theme` `0.1.0-2` were installed.
- Live and installed GRUB theme, lockup, and background files were present and
  byte-identical.
- The v1.0.1 Plymouth theme, symbol, orange dot, white dot, and script were
  present in the generated initramfs; the script uses five animation positions.
- Both approved Obsidian Horizon JPEGs matched their recorded checksums, and
  live/installed dconf and schema defaults selected the branded image for
  light, dark, and lock-screen use.
- Calamares identified the product as OBLinux, used the corrected case-sensitive
  sidebar keys, selected the proportional icon treatment, and contained all
  seven canonical R5 slides.
- `/etc/os-release` identified OBLinux with `ID_LIKE=debian` and
  `LOGO=oblinux-logo`.
- Targeted legacy Debian/OBLinux presentation paths were absent.
- The resulting ISO was recognized as a bootable hybrid ISO 9660 image.
- Repository checks found no broken symlinks, development-machine paths,
  private keys, or token patterns.

## Manual validation still required

Complete `docs/tests/BRAND_MASTER_RUNTIME_TEST.md` in a disposable VM and
record the result. In particular, directly observe GRUB, Plymouth animation,
live GNOME wallpaper, GNOME About, Calamares layout and slides, installation,
installed GRUB/Plymouth, GDM, and the installed GNOME wallpaper. Static payload
inspection does not establish rendering quality or installer success.
