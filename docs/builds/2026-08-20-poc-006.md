# POC Build 006

## Result

- Status: Successful build; runtime acceptance pending
- Build date: 2026-08-20 America/Chicago
- Configuration commit: `ff717ba`
- Base: Debian 13.6 (`trixie`)
- Architecture: `amd64`
- live-build version: `20250505+deb13u1`
- Build duration: approximately 9 minutes 46 seconds

Build 006 addresses the four identity findings recorded during Build 005
acceptance. It remains a proof-of-concept image rather than a supported
release.

## Artifact

| Property | Value |
| --- | --- |
| Filename | `oblinux-debian-gnome-amd64.hybrid.iso` |
| Exact size | 2,135,902,208 bytes |
| Approximate size | 1.99 GiB / 2.14 GB |
| Volume ID | `OBLINUX_LIVE` |
| Package count | 1,766 |
| SHA-256 | `71654bcd52256ee7f5e331d74fb402c7929debf37af27726c9582212e59140ec` |

The ISO, checksum, content and package manifests, file list, and build log are
preserved outside Git under the Build 006 artifact archive.

The image is 11,581,440 bytes, approximately 11.0 MiB, below GitHub Releases'
2 GiB per-file limit.

## Change from Build 005

- Added OBLinux operating-system metadata and the approved application icon
  used by GNOME Settings About.
- Increased Calamares sidebar contrast while retaining the approved palette.
- Added an installed-system OBLinux GRUB theme and concise `OBLinux` menu
  labels.
- Added an OBLinux Plymouth theme with branded logo, background, progress, and
  password-prompt presentation.
- Added guarded build validation for all installed-system identity files.

## Static verification

The following checks passed against the generated build tree:

- `live-build` reported a successful build.
- `/etc/os-release` and `/usr/lib/os-release` are identical and identify
  OBLinux, its Debian base, GNOME variant, and `oblinux-logo` icon.
- The approved scalable OBLinux icon is installed in the hicolor icon theme.
- Calamares uses Slate Blue for its sidebar and Soft White for its text.
- The installed GRUB configuration selects the OBLinux theme and wallpaper.
- Debian's generated GRUB entry logic contains the guarded OBLinux case needed
  for the concise primary and advanced-option labels.
- Plymouth selects the `oblinux` theme.
- Every OBLinux Plymouth asset and its configuration are embedded in the
  generated installed-system initramfs.
- The package manifest remains at 1,766 packages.
- The archived ISO checksum matches the original build output.

## Runtime verification

Runtime acceptance must confirm GNOME About identity, Calamares contrast,
installed GRUB labels and presentation, Plymouth presentation, persistence
after GRUB and initramfs regeneration, installation cleanup, and the existing
regression suite in [Testing](../TESTING.md).
