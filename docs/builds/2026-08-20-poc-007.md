# POC Build 007

## Result

- Status: Successful build; runtime acceptance pending
- Build date: 2026-08-20 America/Chicago
- Configuration commit: `8d0e4c1`
- Base: Debian 13.6 (`trixie`)
- Architecture: `amd64`
- live-build version: `20250505+deb13u1`
- Build duration: approximately 11 minutes 26 seconds

Build 007 is a focused visual-polish image addressing the three
presentation-only findings from Build 006. It remains a proof-of-concept image
rather than a supported release.

## Artifact

| Property | Value |
| --- | --- |
| Filename | `oblinux-debian-gnome-amd64.hybrid.iso` |
| Exact size | 2,135,918,592 bytes |
| Approximate size | 1.99 GiB / 2.14 GB |
| Volume ID | `OBLINUX_LIVE` |
| Package count | 1,766 |
| SHA-256 | `ba6b303950c4e389186667f9fbd3dd7602769e63c71c10b653057de85e396eca` |

The ISO, checksum, content and package manifests, file list, and build log are
preserved outside Git under the Build 007 artifact archive.

The image is 11,565,056 bytes, approximately 11.0 MiB, below GitHub Releases'
2 GiB per-file limit.

## Change from Build 006

- Replaced the opaque rounded-square Calamares logo with the approved reversed
  OBLinux symbol on a transparent 256-pixel canvas.
- Replaced the opaque Plymouth tile with a tightly cropped transparent
  224-by-129-pixel symbol and proportional scaling.
- Added generation checks that reject non-transparent source assets.
- Added a local diversion for Debian's compiled GNOME Control Center vendor
  emblem and installed the approved OBLinux color symbol at the active path.
- Preserved `LOGO=oblinux-logo` and the standard hicolor icon for applications
  that follow `os-release`.

## Static verification

The following checks passed against the generated build tree:

- `live-build` reported `Build completed successfully`.
- The active GNOME vendor emblem matches the approved OBLinux color symbol.
- Debian's original vendor emblem is preserved as `emblem-vendor.svg.distrib`.
- The dpkg database records the expected local vendor-emblem diversion.
- The Calamares logo is a 256-by-256 RGBA PNG matching the approved Build 007
  checksum.
- The Plymouth logo is a 224-by-129 RGBA PNG matching the approved Build 007
  checksum.
- Plymouth scales the logo proportionally rather than forcing a square.
- The corrected Plymouth logo, script, descriptor, and configuration are
  embedded in the generated installed-system initramfs.
- The operating-system name and standard logo identifier remain correct.
- The package manifest remains at 1,766 packages.
- The archived ISO checksum matches the original build output.

## Runtime verification

Runtime acceptance must confirm the OBLinux logo in GNOME Settings About,
artifact-free Calamares and Plymouth symbols, persistence after initramfs
regeneration, installation cleanup, and the retained Build 006 regression
checks in [Testing](../TESTING.md).
