# POC Build 009 Installation Test 01

## Result

- Status: Passed
- Test date: 2026-08-20 America/Chicago
- Image: `oblinux-debian-gnome-amd64.hybrid.iso`
- Image SHA-256:
  `6a844d397d4f08601596c94a76fbe9476713f18b7a2da52876eed8d8802dad19`
- Image build record: [POC Build 009](../builds/2026-08-20-poc-009.md)
- Hypervisor: Oracle VirtualBox on a Linux host
- Test type: UEFI live-session, clean installation, and installed-system audit

## GNOME About badge

The theme-neutral About badge passed in the live system's light and dark
appearances. The following properties were accepted:

- intentional rounded Obsidian Navy background;
- clear Soft White OB letterforms;
- visible Clear Cyan horizon bridge;
- balanced size and spacing;
- clean corners with no clipping or distortion;
- correct OBLinux operating-system identity.

The badge remained present and functional after installation. Build 008's
dark-appearance contrast finding is resolved.

## Regression verification

The following previously accepted presentation surfaces passed without
regression:

- live GRUB;
- live Plymouth;
- GNOME Dash installer icon;
- Calamares sidebar logo and styling;
- installed GRUB;
- installed Plymouth.

All other tested VM functions passed without issue.

## Installed-system audit

A final read-only SSH audit confirmed:

- `/etc/os-release` contains the intended OBLinux identity and Debian
  compatibility metadata.
- The active vendor emblem and canonical About badge have the approved Build
  009 SHA-256 checksum and are byte-identical.
- The badge declares the expected 128-by-128 intrinsic size and rounded
  Obsidian Navy background.
- The local vendor-emblem diversion is registered.
- Debian's original vendor emblem is preserved as
  `emblem-vendor.svg.distrib`.
- No queried installed package matched `calamares*` or `live-*`.
- Calamares branding, its launcher, and the live-only GNOME schema override are
  absent.
- The timezone is `America/Chicago`.
- The clock is synchronized, `systemd-timesyncd` is enabled and active, and the
  hardware clock remains in UTC.
- APT uses only the intended Debian 13, updates, and security repositories.
- Installed GRUB retains the OBLinux distributor, theme, wallpaper, colors,
  and concise title logic.
- Plymouth selects the OBLinux theme.
- `systemctl --failed` reports no failed units.

No changes were made to the installed system during the audit.

## Conclusion

Build 009 passes its complete runtime acceptance. The foundational OBLinux
visual identity is now consistent across live GRUB, live and installed
Plymouth, GNOME desktop wallpaper, Dash installer launcher, Calamares,
installed GRUB, and GNOME Settings About in light and dark appearances.
