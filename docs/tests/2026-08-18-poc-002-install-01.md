# POC Build 002 Installation Test 01

## Result

- Status: Passed with post-install inspection pending
- Test date: 2026-08-18 America/Chicago
- Image: `oblinux-debian-gnome-amd64.hybrid.iso`
- Image SHA-256:
  `561832d2ca01166d61a373dafa14808e2b5aa52a61f2476a0853c7a679b73095`
- Image build record: [POC Build 002](../builds/2026-08-18-poc-002.md)
- Hypervisor: Oracle VirtualBox on a Linux host
- Test type: Manual UEFI erase-disk installation

This is the first complete installation pass for Build 002. A second clean
installation to a newly created virtual disk is still required.

## Reported observations

The manual test passed the following end-to-end sequence:

1. The ISO booted to the GNOME live desktop.
2. The Calamares installer launched successfully.
3. All interactive installer pages completed without a reported error.
4. Calamares reported successful installation.
5. The VM rebooted into the newly installed operating system.
6. The created user logged in to GNOME successfully.
7. Several installed applications were exercised without a reported issue.

This establishes that the basic live-to-installed-system path works. It does not
yet establish that every selected setting or cleanup operation is correct.

## Verification still required

- Confirm the VM booted in UEFI mode.
- Record the GPT, EFI System Partition, and ext4 layout.
- Record the installed GRUB EFI entry.
- Confirm hostname, locale, keyboard, timezone, and user groups.
- Confirm administrative access through `sudo`.
- Run `apt update` and install available updates.
- Confirm live-session packages and credentials are absent.
- Determine whether Calamares or its Debian settings remain installed.
- Check installed APT sources for live-media entries.
- Reboot once after installing updates.
- Locate and retain any available Calamares log.

These inspections can reveal a defect even when the graphical installation and
first boot appear successful.

## Scope limitations

This test does not validate encryption, manual partitioning, dual boot, BIOS
installation, Secure Boot, physical hardware, upgrades, or interrupted-install
recovery.

## Repeatability gate

After post-install inspection passes, repeat the same procedure with the same
ISO and a newly created blank virtual disk. Build 002 completes its installer
gate only after both installations and post-install update checks pass.

